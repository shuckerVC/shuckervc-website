**Input** — single-line text field with an optional label and hint; focus shows the gold ring.

```jsx
<Input label="Work email" type="email" placeholder="you@company.com" />
<Input label="Company" hint="Legal entity name" />
<Input label="Round size" invalid hint="Required" />
```
