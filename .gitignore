# OS / editor
.DS_Store
*.swp
Thumbs.db

# Node
node_modules/

# Local dev / scratch
.claude/serve.js
**/scratchpad/
*.log

# local Claude settings (per-machine)
.claude/settings.local.json

# Internal, non-public config (rubrics, LP posture, decision log).
# This repo is PUBLIC — nothing internal belongs in it, deployed or not.
config/
