**Eyebrow** — the uppercase, gold, rule-prefixed label that sits above section headings.

```jsx
<Eyebrow>Real Value Added</Eyebrow>
<Eyebrow color="inverse">Our Strategy</Eyebrow>
```

Colors: `gold` (default) · `ink` · `muted` · `inverse` (for dark sections).
